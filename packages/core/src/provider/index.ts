/**
 * Provider abstraction and built-in mock provider.
 */

export type { DecisionProvider, DecisionRun } from './types';
export { MockProvider } from './mock';
export type { MockValue, MockProviderOptions } from './mock';