# Support ticket routing example

Categorizes a support ticket, scores its urgency, and checks whether a human is
required, then runs a deterministic workflow to route it.

Runs against the real Jev model.

## Run

```bash
pnpm examples:support
```

Requires a TypeSafe API key in `.env`.